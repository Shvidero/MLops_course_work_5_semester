# MLOps-пайплайн для предсказания тональности отзывов о железнодорожных перевозках

Курсовой проект по дисциплине  
**«Операционализация моделей машинного обучения»**

---

## 📌 Описание проекта

В рамках проекта реализован полный MLOps-пайплайн для задачи классификации тональности текстовых отзывов о железнодорожных перевозках.  
Система позволяет обучать модели, отслеживать эксперименты и использовать обученную модель в виде инференс-сервиса.

Целевая задача — определение тональности текста (`negative / neutral / positive`) на основе пользовательского ввода.

---

## 📊 Данные

- Источник данных: спарсенные посты из сообщества ВК, посвящённого железнодорожным перевозкам
- Объём датасета: ~7000 текстов
- Предобработка:
  - очистка текста
- Разметка тональности выполнена автоматически при помощи предобученной модели ruBERT

---
## 🧠 Используемые модели

- Logistic Regression (основная модель)
- Random Forest (альтернативная модель)
- TF-IDF векторизация текста

Для учёта дисбаланса классов используется параметр `class_weight="balanced"`.

## 🛠 Стек технологий

- Python 3.11.5  
- scikit-learn  
- MLflow  
- FastAPI  
- Docker  
- Streamlit  

---

## 🔁 MLOps-пайплайн
```mermaid
graph TD;
    A[Парсинг постов при помощи VkApi] --> B[EDA и предварительная обработка постов];
    B --> C[Разметка тональности при<br/>помощи предобученной модели RuBERT];
    C --> D[TF-IDF векторизация];
    D --> E[Обучение моделей<br/>LogisticRegression (основная), RandomForest (альтернативная)];
    E --> F[Трэкинг экспериментов (MLflow)];
    F --> G[Инференс-сервис (FastAPI)];
    G --> H[Контейнеризация (Docker)];
    H --> I[Пользовательский интерфейс (UI)];
```
---

## 📁 Структура репозитория

```
.
├── .gitignore
├── app.py # FastAPI инференс-сервис
├── ui.py # UI на Streamlit
├── train.py # обучение моделей
├── config.yaml # конфигурации обучения и инференса
├── requirements.txt # зависимости
├── Dockerfile # Docker-образ сервиса
├── artifacts/
│ ├── model.pkl # обученная модель
│ └── vectorizer.pkl # TF-IDF векторизатор
├── assets/
│ └── image.png
├── Posts.csv # raw data
├── processed_data.csv
├── MODEL_CARD.md
├── DATASET_CARD.md
└── README.md
```

---

## 🚀 Запуск инференс-сервиса (локально)

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```
### 2. Запуск FastAPI
```
uvicorn app:app --reload
```
После запуска доступны:

- Swagger UI:
http://127.0.0.1:8000/docs

- Проверка сервиса:
GET /health

- Предсказание тональности:
POST /predict

## 📥 Пример запроса /predict
```
{
  "texts": [
    "Поездка прошла отлично",
    "Поезд сильно опоздал",
    "В целом нормально"
  ]
}
```
Пример ответа:
```
[
  {"sentiment": 2, "score": 0.88},
  {"sentiment": 0, "score": 0.91},
  {"sentiment": 1, "score": 0.75}
]
```
## 🐳 Запуск через Docker
### Сборка образа
```
docker build -t sentiment-api .
```
### Запуск контейнера
```
docker run -p 8000:8000 sentiment-api
```
Swagger будет доступен по адресу:
http://localhost:8000/docs

## 📈 Трекинг экспериментов
Для отслеживания экспериментов используется MLflow:

- логируются параметры моделей

- логируются метрики качества (accuracy, F1-score)

- сохраняются артефакты моделей и версии датасетов

## 🖥 UI для сервиса
Для демонстрации работы сервиса реализован простой пользовательский интерфейс на Streamlit.

Запуск UI:
```
streamlit run ui.py
```
Интерфейс позволяет:

- вводить несколько текстов

- получать предсказания тональности и уверенность модели

## 📌 Итог
Проект демонстрирует полный цикл операционализации модели машинного обучения:
от подготовки данных и обучения моделей до развёртывания сервиса и пользовательского интерфейса.
